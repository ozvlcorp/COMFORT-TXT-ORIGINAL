# Telegram Bot Token (from @BotFather)
BOT_TOKEN=1234567890:AAxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# MoySklad API Bearer Token
# Получить: МойСклад → Настройки → Пользователи → Ключи доступа
MOYSKLAD_TOKEN=your_moysklad_bearer_token_here

# Webhook server settings (публичный URL для приёма вебхуков от МойСклад)
WEBHOOK_HOST=https://yourdomain.com
WEBHOOK_PATH=/moysklad/webhook
WEBHOOK_PORT=8080

# Secret to validate MoySklad webhook requests
WEBHOOK_SECRET=change_me_to_random_string

# SQLite database file path.
# ВАЖНО: в Docker должно указывать на смонтированный том (/data), иначе
# при пересборке контейнера БД исчезнет вместе с writable-слоем и
# зарегистрированные пользователи потеряются (нужно будет заново /start).
DB_PATH=/data/comfort_bot.db

# Telegram IDs of admins (comma-separated), can broadcast messages
ADMIN_IDS=123456789

# Время дневного отчёта (Asia/Tashkent), default 20:00
# DAILY_REPORT_HOUR=20
# DAILY_REPORT_MINUTE=0

# ── Отчёт по дебиторке (P&L под продажи в долг) ──
# Снимок дебиторки по отгрузкам (sum − payedSum) с разбивкой по срокам
# (текущая / 1–7 / 8–30 / 31–90 / 90+ дней). Шлётся админам вместе с
# дневным отчётом; вручную — командой /debt.
# DEBT_REPORT_ENABLED=1
# Срок оплаты по умолчанию (дни), если у отгрузки нет доп. поля «Срок оплаты»
# DEBT_DEFAULT_TERM_DAYS=30
# Окно сканирования отгрузок в прошлое (дни)
# DEBT_REPORT_LOOKBACK_DAYS=365
# Сколько самых просроченных документов показывать в отчёте
# DEBT_REPORT_TOP_N=15
# Веб-дашборд дебиторки: GET /debt-report?key=<токен>. Пока токен не задан —
# эндпоинт выключен (404), чтобы финансы не были доступны без авторизации.
# DEBT_DASHBOARD_TOKEN=

# Company phone shown in bot description
COMPANY_PHONE=+998958220000

# IANA timezone — botning "hozir" va kalendarlari (momentlar MS qatoridagi raqamlar bilan)
APP_TIMEZONE=Asia/Tashkent

# 1 = har bir hujjat momenti uchun log (raw → saqlangan)
# MS_MOMENT_LOG=1

# MS moment offset siz kelsa: defaultda o‘zgartirilmaydi (none)
# O‘chirish: MOYSKLAD_MOMENT_NAIVE_SOURCE_TZ=none
# MOYSKLAD_MOMENT_NAIVE_SOURCE_TZ=Europe/Moscow

# MoySklad: otgruzka ro'yxati enrich parallelizm (1–16, default 6)
# MOYSKLAD_ENRICH_CONCURRENCY=6
# Webhook workerlar (kichik VPS: 2–3, default 3)
# WEBHOOK_WORKERS=3

# ─── MoySklad rate limiting (defaults respect ~45 req/3s, ~5 parallel) ────────
# MOYSKLAD_MAX_PARALLEL=5              # concurrent requests cap (1–5)
# MOYSKLAD_RATE_LIMIT_MAX_TOKENS=45   # requests per window
# MOYSKLAD_RATE_LIMIT_WINDOW_SEC=3.0  # window length (seconds)
# MOYSKLAD_MAX_RETRIES=4              # transient (5xx/timeout) retry attempts
# MOYSKLAD_MAX_RATE_LIMIT_WAITS=6     # max 429 waits before giving up
# MOYSKLAD_RETRY_BACKOFF_BASE=0.5     # backoff base seconds
# MOYSKLAD_RETRY_BACKOFF_MAX=30.0     # backoff/Retry-After cap seconds

# ─── Caches ───────────────────────────────────────────────────────────────────
# BALANCE_CACHE_TTL_SECONDS=45        # balance cache TTL
# COUNTERPARTY_ID_CACHE_TTL_SECONDS=3600  # phone→counterparty id cache TTL
